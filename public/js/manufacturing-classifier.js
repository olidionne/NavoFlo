/* NavoFlo V8.18.0 — manufacturing / stock-shape classifier.
 * Geometry-only inference. It identifies probable raw stock and secondary
 * machining from exact STEP face metadata + the closed tessellated solid.
 * It never claims manufacturing intent with certainty; results include confidence.
 */
const EPS=1e-9;
const V={
  dot:(a,b)=>a[0]*b[0]+a[1]*b[1]+a[2]*b[2],
  sub:(a,b)=>[a[0]-b[0],a[1]-b[1],a[2]-b[2]],
  scale:(a,s)=>[a[0]*s,a[1]*s,a[2]*s],
  len:a=>Math.hypot(a[0],a[1],a[2]),
  unit(a){const l=this.len(a);return l>EPS?this.scale(a,1/l):null;}
};
function clamp(x,a=0,b=1){return Math.max(a,Math.min(b,x));}
function fam(v){return String(v||'').toLowerCase();}
function canonicalAxis(v){let n=V.unit(v);if(!n)return null;let k=0;for(let i=1;i<3;i++)if(Math.abs(n[i])>Math.abs(n[k]))k=i;if(n[k]<0)n=V.scale(n,-1);return n;}
function pointsOf(geometry){const p=geometry?.positions||[],out=[];for(let i=0;i+2<p.length;i+=3){const q=[Number(p[i]),Number(p[i+1]),Number(p[i+2])];if(q.every(Number.isFinite))out.push(q);}return out;}
function extent(points,axis){let lo=Infinity,hi=-Infinity;for(const p of points){const d=V.dot(p,axis);lo=Math.min(lo,d);hi=Math.max(hi,d);}return hi-lo;}
function solidVolume(geometry){const p=geometry?.positions||[],idx=geometry?.indices||[];if(p.length<9||idx.length<3)return null;let six=0;for(let i=0;i+2<idx.length;i+=3){const ia=Number(idx[i])*3,ib=Number(idx[i+1])*3,ic=Number(idx[i+2])*3;if(ic+2>=p.length)continue;const ax=Number(p[ia]),ay=Number(p[ia+1]),az=Number(p[ia+2]),bx=Number(p[ib]),by=Number(p[ib+1]),bz=Number(p[ib+2]),cx=Number(p[ic]),cy=Number(p[ic+1]),cz=Number(p[ic+2]);if(![ax,ay,az,bx,by,bz,cx,cy,cz].every(Number.isFinite))continue;six+=ax*(by*cz-bz*cy)+ay*(bz*cx-bx*cz)+az*(bx*cy-by*cx);}const v=Math.abs(six/6);return Number.isFinite(v)&&v>EPS?v:null;}
function lineClusters(edgeInfo=[]){const clusters=[];for(const e of edgeInfo){if(!['line','linear'].includes(fam(e.family)))continue;const a=Array.isArray(e.localStartPoint)?e.localStartPoint.map(Number):null,b=Array.isArray(e.localEndPoint)?e.localEndPoint.map(Number):null;if(!a||!b||a.length<3||b.length<3)continue;const d=V.sub(b,a),L=Number(e.length)||V.len(d),axis=canonicalAxis(d);if(!(L>EPS)||!axis)continue;let c=clusters.find(x=>Math.abs(V.dot(x.axis,axis))>=0.9995);if(!c){c={axis,members:[],score:0};clusters.push(c);}c.members.push({edge:e,length:L});c.score+=L;}clusters.sort((a,b)=>b.score-a.score);return clusters;}
function cylinderClusters(faceInfo=[]){const clusters=[];for(const f of faceInfo){if(!['cylinder','cylindrical'].includes(fam(f.family)))continue;const axis=canonicalAxis(Array.isArray(f.axisDirection)?f.axisDirection.map(Number):null),r=Number(f.radius),center=Array.isArray(f.localCenter)?f.localCenter.map(Number).slice(0,3):null,area=Number(f.area)||0;if(!axis||!(r>EPS)||!center?.every(Number.isFinite))continue;let c=clusters.find(x=>Math.abs(V.dot(x.axis,axis))>=0.9995);if(!c){c={axis,members:[],score:0};clusters.push(c);}c.members.push({face:f,r,center,area});c.score+=Math.max(area,1);}clusters.sort((a,b)=>b.score-a.score);return clusters;}
function planeNormalClusters(faceInfo,axis){const clusters=[];for(const f of faceInfo){if(fam(f.family)!=='plane')continue;const n=canonicalAxis(Array.isArray(f.localNormal)?f.localNormal.map(Number):null);if(!n||Math.abs(V.dot(n,axis))>0.08)continue;const area=Number(f.area)||0;let c=clusters.find(x=>Math.abs(V.dot(x.n,n))>=0.995);if(!c){c={n,area:0,count:0};clusters.push(c);}c.area+=area;c.count++;}clusters.sort((a,b)=>b.area-a.area);return clusters;}
function featureSummary(faceInfo,stockAxis,stockRadius=null){let cones=0,tori=0,cylinders=0,transverseCylinders=0,coaxialOtherRadii=0,planes=0;for(const f of faceInfo){const g=fam(f.family);if(g==='cone')cones++;else if(g==='torus')tori++;else if(g==='plane')planes++;else if(['cylinder','cylindrical'].includes(g)){cylinders++;const a=canonicalAxis(Array.isArray(f.axisDirection)?f.axisDirection.map(Number):null);if(a&&stockAxis){const align=Math.abs(V.dot(a,stockAxis));if(align<0.98)transverseCylinders++;else if(Number.isFinite(stockRadius)&&Math.abs(Number(f.radius)-stockRadius)>Math.max(stockRadius*0.01,1e-4))coaxialOtherRadii++;}}}
  const hints=[];if(coaxialOtherRadii>0)hints.push('turning');if(transverseCylinders>0)hints.push('drilling');if(cones>0)hints.push('chamfering');if(tori>0)hints.push('fillets');return{cones,tori,cylinders,transverseCylinders,coaxialOtherRadii,planes,hints};}
function roundCandidate(geometry,faceInfo,points,volume){let best=null;for(const cluster of cylinderClusters(faceInfo)){const outer=cluster.members.slice().sort((a,b)=>b.r-a.r)[0];if(!outer)continue;const axis=cluster.axis,R=outer.r,L=extent(points,axis);if(!(L>EPS&&R>EPS))continue;let maxRad=0;for(const p of points){const rel=V.sub(p,outer.center),t=V.dot(rel,axis),rad=V.len(V.sub(rel,V.scale(axis,t)));maxRad=Math.max(maxRad,rad);}const envelopeError=Math.abs(maxRad-R)/R;if(envelopeError>0.08)continue;const stockVolume=Math.PI*R*R*L,volumeRatio=volume/stockVolume;if(!(volumeRatio>=0.12&&volumeRatio<=1.03))continue;const features=featureSummary(faceInfo,axis,R),removal=clamp(1-volumeRatio,0,1);const machined=removal>0.003||features.cones>0||features.tori>0||features.transverseCylinders>0||features.coaxialOtherRadii>0||features.planes>2;const confidence=clamp(0.98-envelopeError*2.5-Math.max(0,0.18-volumeRatio)*0.4);const c={stockType:'round-bar',axis,lengthMm:L,diameterMm:2*R,stockVolume,volumeRatio,materialRemoval:removal,machined,features,confidence,aspect:L/(2*R)};if(!best||c.confidence>best.confidence)best=c;}return best;}
function rectangularCandidate(geometry,faceInfo,edgeInfo,points,volume){const line=lineClusters(edgeInfo)[0];if(!line||line.members.length<2)return null;const axis=line.axis,L=extent(points,axis);if(!(L>EPS))return null;const normals=planeNormalClusters(faceInfo,axis);if(normals.length<2)return null;const u=normals[0].n,vEntry=normals.slice(1).find(c=>Math.abs(V.dot(u,c.n))<0.12);if(!vEntry)return null;const v=vEntry.n,w=extent(points,u),h=extent(points,v);if(!(w>EPS&&h>EPS))return null;const stockVolume=L*w*h,volumeRatio=volume/stockVolume;if(!(volumeRatio>=0.15&&volumeRatio<=1.03))return null;const major=Math.max(w,h),minor=Math.min(w,h),crossRatio=major/minor,aspect=L/major;let stockType='rectangular-bar';if(crossRatio<=1.08)stockType='square-bar';else if(crossRatio>=2.5)stockType='flat-bar';const features=featureSummary(faceInfo,axis,null),removal=clamp(1-volumeRatio,0,1);const expectedPlaneCount=6,machined=removal>0.003||features.cylinders>0||features.cones>0||features.tori>0||features.planes>expectedPlaneCount;const confidence=clamp(0.92-Math.max(0,0.4-volumeRatio)*0.5+Math.min(aspect,5)*0.01);return{stockType,axis,lengthMm:L,widthMm:major,thicknessMm:minor,stockVolume,volumeRatio,materialRemoval:removal,machined,features,confidence,aspect,normalFamilyCount:normals.length};}
function hexCandidate(geometry,faceInfo,edgeInfo,points,volume){const line=lineClusters(edgeInfo)[0];if(!line||line.members.length<4)return null;const axis=line.axis,L=extent(points,axis),normals=planeNormalClusters(faceInfo,axis);if(!(L>EPS)||normals.length<3)return null;const dirs=[];for(const c of normals){if(dirs.every(d=>Math.abs(V.dot(d,c.n))<0.995))dirs.push(c.n);if(dirs.length===3)break;}if(dirs.length!==3)return null;const widths=dirs.map(n=>extent(points,n)),mean=widths.reduce((a,b)=>a+b,0)/3;if(!(mean>EPS))return null;const spread=(Math.max(...widths)-Math.min(...widths))/mean;if(spread>0.08)return null;const angles=[];for(let i=0;i<3;i++)for(let j=i+1;j<3;j++)angles.push(Math.acos(clamp(Math.abs(V.dot(dirs[i],dirs[j])),-1,1))*180/Math.PI);if(!angles.every(a=>a>50&&a<70))return null;const area=Math.sqrt(3)/2*mean*mean,stockVolume=area*L,volumeRatio=volume/stockVolume;if(!(volumeRatio>=0.15&&volumeRatio<=1.04))return null;const features=featureSummary(faceInfo,axis,null),removal=clamp(1-volumeRatio,0,1),machined=removal>0.003||features.cylinders>0||features.cones>0||features.tori>0||features.planes>8;return{stockType:'hex-bar',axis,lengthMm:L,acrossFlatsMm:mean,stockVolume,volumeRatio,materialRemoval:removal,machined,features,confidence:clamp(0.94-spread*2),aspect:L/mean};}
function score(c){if(!c)return-Infinity;let s=Number(c.confidence)||0;if(c.stockType==='round-bar')s+=0.03;return s;}
export function classifyManufacturingGeometry({geometry,faceInfo=[],edgeInfo=[]}={}){
  const points=pointsOf(geometry),volume=solidVolume(geometry);if(points.length<4||!(volume>EPS))return null;
  const candidates=[roundCandidate(geometry,faceInfo,points,volume),hexCandidate(geometry,faceInfo,edgeInfo,points,volume),rectangularCandidate(geometry,faceInfo,edgeInfo,points,volume)].filter(Boolean);if(!candidates.length)return null;candidates.sort((a,b)=>score(b)-score(a));const best=candidates[0];
  const evidence=[];if(best.features?.hints?.length)evidence.push(...best.features.hints);if(best.materialRemoval>0.003)evidence.push('material-removal');
  return{...best,kind:'stock-shape',process:best.machined?'machining':'stock-profile',evidence:[...new Set(evidence)],volumeMm3:volume};
}
